'use client'
import LoginForm from '@/components/template/LoginForm'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div><LoginForm />
        </div>
    )
}

export default page